SQLI|union select
SQLI|or 1=1
SQLI|information_schema
SQLI|sleep(
SQLI|benchmark(

XSS|<script>
XSS|javascript:
XSS|onerror=
XSS|alert(

PATH|../
PATH|..\\
PATH|/etc/passwd

CMD|cmd.exe
CMD|/bin/sh
CMD|powershell

SCAN|sqlmap
SCAN|nikto
SCAN|acunetix

BOT|curl/
BOT|python-requests
BOT|wget

UPLOAD|multipart/form-data
UPLOAD|filename=
